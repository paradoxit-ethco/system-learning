import { useEffect, useMemo, useState } from "react";
import { ArrowRight, Bell, BookOpen, Bot, BrainCircuit, Check, ChevronRight, Code2, Compass, FileText, Gamepad2, Globe2, Heart, Lightbulb, Menu, MessageSquare, MoreHorizontal, Palette, Play, Plus, Rocket, Search, Settings, Sparkles, Target, Trophy, User, Wand2, X, Zap } from "lucide-react";
type Icon = typeof Sparkles;
const nav = [["Home", "/", Sparkles], ["Explore", "/explore", Compass], ["Study", "/study", BookOpen], ["Coding", "/coding", Code2], ["AI Tools", "/ai-tools", Bot], ["Projects", "/projects", Rocket], ["About", "/about", Heart], ["Contact", "/contact", MessageSquare]] as [string, string, Icon][];
const featureCards = [{ title: "AI Tools", text: "Discover useful AI-powered tools.", icon: Bot, color: "violet", route: "/ai-tools" }, { title: "Study & Exams", text: "Learn, practice, and prepare for exams.", icon: BookOpen, color: "blue", route: "/study" }, { title: "Coding", text: "Learn programming and build websites.", icon: Code2, color: "cyan", route: "/coding" }, { title: "Creative Studio", text: "Turn your ideas into beautiful creations.", icon: Palette, color: "pink", route: "/creative" }, { title: "Mini Games", text: "Relax with interactive games.", icon: Gamepad2, color: "orange", route: "/games" }, { title: "My Projects", text: "Manage and showcase your projects.", icon: Rocket, color: "green", route: "/projects" }];
const exploreItems = featureCards.map((x, i) => ({ ...x, category: ["AI", "Education", "Coding", "Creative", "Games", "Projects"][i], tag: ["NEW", "POPULAR", "PATH", "STUDIO", "PLAY", "YOUR SPACE"][i] }));
const subjects = ["English", "Mathematics", "Biology", "Chemistry", "Physics", "Computer Science"];
const quiz = [{ q: "What does HTML stand for?", a: ["Hyper Text Markup Language", "High Tech Modern Logic", "Home Tool Markup Link"], correct: 0 }, { q: "Which planet is known as the Red Planet?", a: ["Venus", "Mars", "Jupiter"], correct: 1 }, { q: "What is 12 × 8?", a: ["86", "96", "108"], correct: 1 }];
function go(route: string) { history.pushState({}, "", route); window.dispatchEvent(new PopStateEvent("popstate")); window.scrollTo({ top: 0, behavior: "smooth" }); }
function Button({ children, onClick, variant = "primary", className = "" }: { children: React.ReactNode; onClick?: () => void; variant?: "primary" | "ghost" | "soft"; className?: string }) { return <button className={`button ${variant} ${className}`} onClick={onClick}>{children}</button>; }
function IconView({ I, size = 18 }: { I: any; size?: number }) { const C = I; return <C size={size} />; }
function Glass({ children, className = "", id }: { children: React.ReactNode; className?: string; id?: string }) { return <div id={id} className={`glass ${className}`}>{children}</div>; }
function Header({ setMobile }: { setMobile: (v: boolean) => void }) { const [path, setPath] = useState(location.pathname); useEffect(() => { const f = () => setPath(location.pathname); addEventListener("popstate", f); return () => removeEventListener("popstate", f); }, []); return <header className="topbar"><button className="mobile-toggle" onClick={() => setMobile(true)} aria-label="Open menu"><Menu size={20} /></button><button className="brand" onClick={() => go("/")}><span className="brand-mark">✦</span><span>DREAMY <b>HUB</b></span></button><nav>{nav.map(([label, route]) => <button key={route} className={path === route ? "active" : ""} onClick={() => go(route)}>{label}</button>)}</nav><div className="header-actions"><button className="icon-btn"><Search size={17} /></button><button className="icon-btn"><Bell size={17} /><i /></button><button className="avatar" onClick={() => go("/profile")}>DH</button></div></header>; }
function MobileMenu({ open, setOpen }: { open: boolean; setOpen: (v: boolean) => void }) { return open ? <div className="mobile-overlay"><div className="mobile-drawer"><div className="drawer-head"><span className="brand"><span className="brand-mark">✦</span>DREAMY <b>HUB</b></span><button onClick={() => setOpen(false)}><X /></button></div>{nav.map(([label, route, I]) => <button key={route} onClick={() => { setOpen(false); go(route); }}><I size={18} />{label}<ChevronRight size={15} /></button>)}</div></div> : null; }
function BottomNav() { return <div className="bottom-nav">{[["/", Sparkles, "Home"], ["/explore", Compass, "Explore"], ["/creative", Plus, "Create"], ["/projects", Rocket, "Projects"], ["/profile", User, "Profile"]].map(([r, I, l]) => <button key={r as string} onClick={() => go(r as string)}><IconView I={I} size={18} /><span>{l as string}</span></button>)}</div>; }
function PageIntro({ eyebrow, title, text, action }: { eyebrow: string; title: React.ReactNode; text: string; action?: React.ReactNode }) { return <div className="page-intro"><div><span className="eyebrow">{eyebrow}</span><h1>{title}</h1><p>{text}</p></div>{action}</div>; }
function Stat({ value, label }: { value: string; label: string }) { return <div className="stat"><strong>{value}</strong><span>{label}</span></div>; }
function Home() { return <><section className="hero"><div className="hero-copy"><div className="eyebrow"><span className="live-dot" /> THE CREATOR'S OPERATING SYSTEM</div><h1>Your ideas<br /><em>start here.</em></h1><p>Everything you need to learn, create, and turn your ideas into real projects — beautifully in one place.</p><div className="hero-actions"><Button onClick={() => go("/explore")}>Start exploring <ArrowRight size={17} /></Button><Button variant="ghost" onClick={() => go("/projects")}>View projects <span className="shortcut">⌘ K</span></Button></div><div className="trusted"><div className="mini-avatars"><span>AK</span><span>JM</span><span>NO</span><span>+</span></div><span>Join 12,000+ curious minds building their next thing.</span></div></div><div className="hero-art"><div className="orb orb-one" /><div className="orb orb-two" /><div className="orbit orbit-a" /><div className="orbit orbit-b" /><div className="hero-card glass"><div className="sparkline"><span>IDEA</span><span>BUILD</span><span>SHIP</span></div><div className="wave" /><div className="hero-card-foot"><span><span className="tiny-dot" /> Focus mode</span><b>08:42</b></div></div><div className="floating-chip chip-top"><Sparkles size={14} /> Momentum <b>+24%</b></div><div className="floating-chip chip-bottom"><Target size={15} /> 3 day streak</div></div></section><section className="section"><div className="section-head"><div><span className="eyebrow">A LITTLE BIT OF EVERYTHING</span><h2>Built for the <span>in-between.</span></h2></div><button className="text-link" onClick={() => go("/explore")}>Explore all <ArrowRight size={15} /></button></div><div className="feature-grid">{featureCards.map(({ title, text, icon: I, color, route }) => <button className="feature-card glass" key={title} onClick={() => go(route)}><div className={`feature-icon ${color}`}><I size={19} /></div><div><h3>{title}</h3><p>{text}</p></div><ChevronRight className="feature-arrow" size={17} /></button>)}</div></section><section className="dashboard-row"><Glass className="progress-panel"><div className="panel-head"><div><span className="eyebrow">YOUR MOMENTUM</span><h2>Keep the <span>streak alive.</span></h2></div><MoreHorizontal size={18} /></div><div className="progress-content"><div className="progress-ring"><div><strong>68%</strong><small>complete</small></div></div><div className="progress-list"><div><span><Check size={13} /> HTML Foundations</span><b>100%</b></div><div><span><Check size={13} /> Design systems</span><b>74%</b></div><div><span className="pending"><Zap size={13} /></span> JavaScript basics</div><div><span className="pending"><Zap size={13} /></span> Ship your first project</div></div></div><button className="panel-link" onClick={() => go("/study")}>Continue learning <ArrowRight size={15} /></button></Glass><Glass className="assistant-panel"><div className="assistant-glow"><Bot size={22} /></div><span className="eyebrow">MEET YOUR CO-PILOT</span><h2>Got a thought?<br /><span>Let's make it real.</span></h2><p>Ask anything, brainstorm ideas, or get unstuck in seconds.</p><Button variant="soft" onClick={() => go("/ai-tools")}>Talk to Dreamy <MessageSquare size={15} /></Button><div className="assistant-orb">✦</div></Glass></section><section className="cta"><div><span className="eyebrow">NO MORE BLANK PAGES</span><h2>Ready to build<br /><span>something amazing?</span></h2></div><Button onClick={() => go("/explore")}>Enter DREAMY HUB <ArrowRight size={17} /></Button></section></>; }
function Explore() { const [filter, setFilter] = useState("All"); const [query, setQuery] = useState(""); const items = exploreItems.filter(x => (filter === "All" || x.category === filter) && x.title.toLowerCase().includes(query.toLowerCase())); return <><PageIntro eyebrow="THE WHOLE UNIVERSE" title={<>Explore <span>everything.</span></>} text="Find your next rabbit hole, favorite tool, or the spark for your next big idea." /><div className="explore-tools"><div className="search-field"><Search size={17} /><input placeholder="Search the hub..." value={query} onChange={e => setQuery(e.target.value)} /><kbd>⌘ K</kbd></div><div className="filters">{["All", "AI", "Education", "Coding", "Creative", "Games", "Projects"].map(x => <button className={filter === x ? "selected" : ""} onClick={() => setFilter(x)} key={x}>{x}</button>)}</div></div><div className="explore-grid">{items.map(({ title, text, icon: I, color, category, tag, route }) => <Glass className="explore-card" key={title}><div className="card-top"><div className={`feature-icon ${color}`}><I size={19} /></div><span className="tag">{tag}</span></div><h3>{title}</h3><p>{text}</p><div className="card-bottom"><span>{category}</span><button onClick={() => go(route)}>Open <ArrowRight size={14} /></button></div></Glass>)}</div></>; }
function AITools() { const [messages, setMessages] = useState(["Hey there. What are we making today?"]); const [input, setInput] = useState(""); const send = () => { if (!input.trim()) return; setMessages([...messages, input, "Love that direction. I’d start with a tiny first version, then iterate from there. ✦"]); setInput(""); }; const toolData = [["AI Chat", Bot, "A thinking partner for your wildest questions."], ["AI Writing", FileText, "Clear, compelling words in your voice."], ["Study Assistant", BookOpen, "Turn confusion into a study plan."], ["Idea Generator", Lightbulb, "Never run out of what to make next."], ["Code Helper", Code2, "Ship cleaner code, faster."], ["Summarizer", BrainCircuit, "The short version, without the fluff."]]; return <><PageIntro eyebrow="YOUR CREATIVE CO-PILOT" title={<>Work smarter.<br /><span>Create faster.</span></>} text="A focused collection of AI tools to help you move from blank page to breakthrough." /><div className="tool-grid">{toolData.map(([x, I, text], i) => <Glass className="tool-card" key={x as string}><div className={`tool-symbol s${i}`}><IconView I={I} size={20} /></div><h3>{x as string}</h3><p>{text as string}</p><button onClick={() => document.getElementById("ai-chat")?.scrollIntoView({ behavior: "smooth" })}>Try now <ArrowRight size={14} /></button></Glass>)}</div><Glass className="chat-box" id="ai-chat"><div className="chat-head"><div className="assistant-glow"><Bot size={19} /></div><div><b>Dreamy assistant</b><span><i /> Online · ready when you are</span></div><span className="api-note">Frontend preview</span></div><div className="chat-messages">{messages.map((m, i) => <div className={i % 2 ? "message user" : "message bot"} key={i}>{i % 2 === 0 && <Bot size={15} />}<span>{m}</span></div>)}</div><div className="suggestions">{["Help me brainstorm", "Explain this simply", "Make me a study plan"].map(x => <button key={x} onClick={() => setInput(x)}>{x}</button>)}</div><div className="chat-input"><input value={input} onChange={e => setInput(e.target.value)} onKeyDown={e => e.key === "Enter" && send()} placeholder="Ask Dreamy anything..." /><button onClick={send}><ArrowRight size={17} /></button></div></Glass></>; }
function Study() { const [q, setQ] = useState(0); const [score, setScore] = useState(0); const [picked, setPicked] = useState<number | null>(null); const answer = (i: number) => { if (picked !== null) return; setPicked(i); if (i === quiz[q].correct) setScore(score + 1); }; return <><PageIntro eyebrow="LEARN AT YOUR PACE" title={<>Make progress.<br /><span>Feel the click.</span></>} text="Small sessions. Clear goals. A little more confident every day." action={<Button onClick={() => document.getElementById("quiz")?.scrollIntoView({ behavior: "smooth" })}>Start a quiz <Play size={15} /></Button>} /><div className="subject-grid">{subjects.map((x, i) => <Glass className="subject-card" key={x}><div className={`subject-num n${i}`}>0{i + 1}</div><div><h3>{x}</h3><p>{["12 lessons · 2h 40m", "8 lessons · 1h 15m", "16 lessons · 3h 20m", "10 lessons · 2h 05m", "14 lessons · 2h 50m", "20 lessons · 4h 10m"][i]}</p></div><ChevronRight size={16} /></Glass>)}</div><Glass className="quiz-card" id="quiz"><div className="quiz-side"><span className="eyebrow">QUICK CHECK</span><h2>Test your<br /><span>knowledge.</span></h2><div className="quiz-progress"><i style={{ width: `${((q + 1) / quiz.length) * 100}%` }} /></div><small>Question {q + 1} of {quiz.length} · Score {score}</small></div><div className="quiz-main"><span className="eyebrow">QUESTION {String(q + 1).padStart(2, "0")}</span><h3>{quiz[q].q}</h3><div className="answers">{quiz[q].a.map((a, i) => <button className={picked === i ? i === quiz[q].correct ? "correct" : "wrong" : ""} onClick={() => answer(i)} key={a}><span>{String.fromCharCode(65 + i)}</span>{a}{picked === i && (i === quiz[q].correct ? <Check size={15} /> : <X size={15} />)}</button>)}</div><Button variant="soft" className="next-btn" onClick={() => { setQ((q + 1) % quiz.length); setPicked(null); }}>Next question <ArrowRight size={15} /></Button></div></Glass></>; }
function Coding() { const [tab, setTab] = useState("HTML"); const [code, setCode] = useState("<h1>Hello, DREAMY</h1>\n<p>Your first idea starts here.</p>"); const preview = tab === "HTML" ? code : tab === "CSS" ? "body {\n  background: #101015;\n  color: #fff;\n}" : "const idea = 'ship it';\nconsole.log(idea);"; return <><PageIntro eyebrow="MAKE THINGS THAT MOVE" title={<>Learn to <span>code.</span></>} text="From your first tag to your first shipped product. Learn by making things that feel like you." /><div className="path-row">{["HTML", "CSS", "JavaScript", "Python", "C", "C++"].map((x, i) => <Glass className="path-card" key={x}><div className="path-icon">{x === "JavaScript" ? "JS" : x === "Python" ? "Py" : "<>"}</div><b>{x}</b><span>{i < 3 ? "Beginner" : "Coming soon"}</span></Glass>)}</div><Glass className="editor"><div className="editor-head"><div className="tabs">{["HTML", "CSS", "JS"].map(x => <button className={tab === x || (x === "JS" && tab === "JavaScript") ? "selected" : ""} onClick={() => { setTab(x === "JS" ? "JavaScript" : x); setCode(x === "HTML" ? "<h1>Hello, DREAMY</h1>\n<p>Your first idea starts here.</p>" : x === "CSS" ? "body {\n  background: #101015;\n  color: #fff;\n}" : "const idea = 'ship it';\nconsole.log(idea);"); }} key={x}>{x}</button>)}</div><Button variant="soft"><Play size={14} /> Run</Button></div><div className="editor-grid"><div className="code-area"><span className="line-numbers">1<br />2<br />3<br />4<br />5</span><textarea value={code} onChange={e => setCode(e.target.value)} spellCheck={false} /></div><div className="preview"><div className="preview-label"><Globe2 size={13} /> PREVIEW</div>{tab === "HTML" ? <div dangerouslySetInnerHTML={{ __html: code }} /> : <pre>{preview}</pre>}</div></div></Glass></>; }
function Creative() { const [notes, setNotes] = useState<string[]>(() => JSON.parse(localStorage.getItem("dreamy-notes") || "[]")); const [draft, setDraft] = useState(""); const add = () => { if (!draft.trim()) return; const next = [draft, ...notes]; setNotes(next); localStorage.setItem("dreamy-notes", JSON.stringify(next)); setDraft(""); }; return <><PageIntro eyebrow="A SPACE TO PLAY" title={<>Make room for<br /><span>good ideas.</span></>} text="A softer corner of DREAMY HUB for collecting sparks, shaping thoughts, and making something just because you can." /><div className="creative-grid"><Glass className="idea-maker"><div className="assistant-glow"><Wand2 size={19} /></div><span className="eyebrow">SPARK GENERATOR</span><h2>What if you<br /><span>started with why?</span></h2><p>Sometimes the best idea is one good question away.</p><Button onClick={() => setDraft("A tiny tool that helps people make space for their best ideas.")}>Give me a spark <Sparkles size={15} /></Button></Glass><Glass className="notes-panel"><div className="panel-head"><div><span className="eyebrow">YOUR SCRATCHPAD</span><h2>Loose thoughts.</h2></div><FileText size={19} /></div><div className="note-input"><input value={draft} onChange={e => setDraft(e.target.value)} onKeyDown={e => e.key === "Enter" && add()} placeholder="Write a thought..." /><button onClick={add}><Plus size={17} /></button></div><div className="notes-list">{notes.length ? notes.map((n, i) => <div key={i}><span>0{i + 1}</span>{n}</div>) : <div className="empty-note">Your next idea could live here.</div>}</div></Glass></div><div className="creative-tools">{[["Color Palette", Palette, "Find a mood", "pink"], ["Design Inspiration", Heart, "A little visual fuel", "blue"], ["Text Generator", FileText, "Find your words", "violet"], ["Project Planner", Target, "Make it a plan", "green"]].map(([x, I, sub, color]) => <Glass key={x as string}><div className={`feature-icon ${color}`}><IconView I={I} size={18} /></div><b>{x as string}</b><span>{sub as string}</span><ArrowRight size={14} /></Glass>)}</div></>; }
function Games() { const [number, setNumber] = useState(Math.ceil(Math.random() * 10)); const [guess, setGuess] = useState(""); const [message, setMessage] = useState("Guess a number between 1 and 10."); const [best, setBest] = useState(() => Number(localStorage.getItem("dreamy-best") || 0)); const [tries, setTries] = useState(0); const check = () => { const n = Number(guess); if (!n) return; const t = tries + 1; setTries(t); if (n === number) { setMessage("You got it! New round unlocked."); if (!best || t < best) { setBest(t); localStorage.setItem("dreamy-best", String(t)); } } else setMessage(n > number ? "A little lower..." : "Go a little higher..."); }; return <><PageIntro eyebrow="TAKE FIVE" title={<>Small games.<br /><span>Big break.</span></>} text="A little playful reset between big thoughts. Your best scores stay right here." /><div className="games-grid"><Glass className="game-feature"><div className="game-meta"><span className="eyebrow">NUMBER GUESS</span><Trophy size={18} /></div><h2>Can you read<br /><span>the signal?</span></h2><p>{message}</p><div className="guess-row"><input type="number" value={guess} onChange={e => setGuess(e.target.value)} placeholder="?" /><Button onClick={check}>Check <ArrowRight size={15} /></Button></div><div className="score-row"><span>Attempts <b>{tries}</b></span><span>Best <b>{best || "—"}</b></span><button onClick={() => { setNumber(Math.ceil(Math.random() * 10)); setGuess(""); setTries(0); setMessage("Guess a number between 1 and 10."); }}>Restart</button></div></Glass><div className="game-list">{[["Memory Match", "Find the pairs", Gamepad2], ["Quick Quiz", "How fast can you think?", BrainCircuit], ["Reaction Test", "Test your reflexes", Zap]].map(([x, sub, I], i) => <Glass key={x as string}><div className={`game-icon g${i}`}><IconView I={I} size={18} /></div><div><b>{x as string}</b><span>{sub as string}</span></div><ChevronRight size={16} /></Glass>)}</div></div></>; }
function Projects() { const [projects, setProjects] = useState<{ name: string; category: string; progress: number; color: string }[]>(() => JSON.parse(localStorage.getItem("dreamy-projects") || "null") || [{ name: "Personal portfolio", category: "Web · Design", progress: 72, color: "violet" }, { name: "Study sprint", category: "Learning", progress: 44, color: "blue" }, { name: "Tiny weather app", category: "Code", progress: 18, color: "green" }]); const [show, setShow] = useState(false); const [name, setName] = useState(""); const add = () => { if (!name.trim()) return; const next = [{ name, category: "New project", progress: 0, color: "pink" }, ...projects]; setProjects(next); localStorage.setItem("dreamy-projects", JSON.stringify(next)); setName(""); setShow(false); }; return <><PageIntro eyebrow="YOUR WORK, YOUR WAY" title={<>Ideas in progress.<br /><span>Keep going.</span></>} text="A home for the things you’re building, learning, and not quite ready to call finished." action={<Button onClick={() => setShow(true)}><Plus size={16} /> New project</Button>} /><div className="project-summary"><Stat value={String(projects.length).padStart(2, "0")} label="active projects" /><Stat value="12" label="days building" /><Stat value="84%" label="avg. momentum" /><div className="summary-note"><Sparkles size={15} /> You’re on a roll.</div></div><div className="projects-list">{projects.map((p: { name: string; category: string; progress: number; color: string }, i: number) => <Glass className="project-card" key={i}><div className={`project-cover ${p.color}`}><span>{p.name.slice(0, 2).toUpperCase()}</span><MoreHorizontal size={18} /></div><div className="project-details"><div><span className="eyebrow">{p.category}</span><h3>{p.name}</h3></div><button onClick={() => alert("Project workspace preview")}>Open <ArrowRight size={14} /></button></div><div className="project-progress"><div><span>Progress</span><b>{p.progress}%</b></div><div className="bar"><i style={{ width: `${p.progress}%` }} /></div></div></Glass>)}</div>{show && <div className="modal-wrap"><Glass className="modal"><button className="modal-close" onClick={() => setShow(false)}><X size={18} /></button><span className="eyebrow">NEW BEGINNING</span><h2>Start a project.</h2><p>Give your next idea a place to grow.</p><input autoFocus value={name} onChange={e => setName(e.target.value)} onKeyDown={e => e.key === "Enter" && add()} placeholder="Project name" /><Button onClick={add}>Create project <ArrowRight size={15} /></Button></Glass></div>}</>; }
function SimplePage({ kind }: { kind: string }) { const info: Record<string, [string, string, string]> = { about: ["THE HEART OF THE HUB", "About DREAMY HUB.", "Learning, creativity, technology, and personal projects — in one simple place made for curious people."], contact: ["WE’RE ALL EARS", "Let’s talk.", "Questions, ideas, feedback, or just a hello. Send a note and we’ll find our way back to you."], profile: ["YOUR SPACE", "Hey, dreamer.", "A little home base for the things you’re learning, making, and becoming."], settings: ["MAKE IT YOURS", "Your settings.", "Tune your experience. Keep the signal, lose the noise."] }; const [ey, title, text] = info[kind] || info.about; return <><PageIntro eyebrow={ey} title={<>{title}</>} text={text} /><div className="simple-grid"><Glass><div className="large-symbol">{kind === "about" ? <Heart /> : kind === "contact" ? <MessageSquare /> : kind === "profile" ? <User /> : <Settings />}</div><span className="eyebrow">A NOTE FROM DREAMY</span><h2>{kind === "about" ? "Made for the curious." : kind === "contact" ? "What’s on your mind?" : kind === "profile" ? "You’re doing better than you think." : "Small changes, better focus."}</h2><p>{kind === "about" ? "DREAMY HUB brings the useful, the playful, and the inspiring into one calm digital space — so your next idea has somewhere to land." : "The best work happens when you have room to ask questions, try things, and begin again."}</p><Button onClick={() => go(kind === "contact" ? "/ai-tools" : "/explore")}>{kind === "contact" ? "Start a conversation" : "Explore the hub"} <ArrowRight size={15} /></Button></Glass><div className="values">{(kind === "about" ? [["01", "Learn", "Stay curious. Keep asking better questions."], ["02", "Create", "Make space for ideas without judging them too early."], ["03", "Build", "Turn small experiments into something real."], ["04", "Explore", "Follow the thread. See where it takes you."]] : [["Focus", "A calmer place for deep work."], ["Momentum", "Small steps count here."], ["Possibility", "There’s always another way in."]]).map((x: any) => <Glass key={x[0]}><span className="value-num">{x[0]}</span><h3>{x[1]}</h3><p>{x[2]}</p></Glass>)}</div></div></>; }
function App() { const [mobile, setMobile] = useState(false); const [path, setPath] = useState(location.pathname); useEffect(() => { const f = () => setPath(location.pathname); addEventListener("popstate", f); return () => removeEventListener("popstate", f); }, []); const page = useMemo(() => { if (path === "/") return <Home />; if (path === "/explore") return <Explore />; if (path === "/ai-tools") return <AITools />; if (path === "/study") return <Study />; if (path === "/coding") return <Coding />; if (path === "/creative") return <Creative />; if (path === "/games") return <Games />; if (path === "/projects") return <Projects />; return <SimplePage kind={path.slice(1)} />; }, [path]); return <div className="app"><Header setMobile={setMobile} /><main className={path === "/" ? "home-main" : "page-main"}>{page}</main><footer><span>✦ DREAMY HUB</span><span>Made for the curious · 2026</span><span>Privacy · Terms · <button onClick={() => go("/contact")}>Contact</button></span></footer><MobileMenu open={mobile} setOpen={setMobile} /><BottomNav /></div>; }
export default App;
